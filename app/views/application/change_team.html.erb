<% if current_user.present? && current_user.is_admin %>
    <% @user.update(team_id: @team_id) %>
    <script type="text/javascript">
        location.replace("/admin#menu1")
    </script>
    Switched to Role Admin

    <%=link_to 'Zurück', admin_path %>
<% end %>